#include <iostream>
#include <string>

using namespace std;

int main()
{
    int tests;
    cin>>tests;

    while(tests--){
        int len;
        cin>>len;
        int nums[len];
        
        for(int i = 0; i < len; ++i){
           cin>>nums[i]; 
        }

        

        //cout<<count<<"\n";
    }
}